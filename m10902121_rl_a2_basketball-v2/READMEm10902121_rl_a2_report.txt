Add a README.txt file into the directory, which shows:
 
(a) install the packages below by using pip install
pip install matplotlib
pip install gym
pip install numpy
pip install torch
pip install tqdm
pip install Tkinter


(b) how to run the code
1)  we need to make the random enviroment via maze_rdn.ipynb
2)  run: python maze_5actions.py
3)  If you want to play on your own, then run: python maze_forppl.py